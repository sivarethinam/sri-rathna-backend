# Server
server.port=${SERVER_PORT:8080}

# Supabase PostgreSQL
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# App Config
app.admin.mobile=9999999999
app.otp.expiry-minutes=10
app.advance-amount=1000

# Logging
logging.level.com.srirathna=INFO
```

Click **"Commit changes"** → Render will **automatically redeploy**.

---

## ⏳ STEP 3 — Wait for Build

- Build takes about **5-10 minutes** first time
- You'll see logs in Render dashboard
- Wait until you see:
```
  Started SriRathnaApplication in X seconds
```
- Your backend URL will be:
```
  https://sri-rathna-backend.onrender.com
```

---

## ✅ STEP 4 — Test Backend is Live

Open this URL in browser (replace with your actual Render URL):
```
https://sri-rathna-backend.onrender.com/api/bookings/booked-dates
