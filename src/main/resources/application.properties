# Sri Rathna Marriage Hall - Application Properties

# Server
server.port=8080

# Supabase PostgreSQL Connection
# Replace with your actual Supabase DB password
spring.datasource.url=jdbc:postgresql://db.pbgiqhsvhrflbbzadmdi.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=VQnQG9VF1xgEaQkB
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret (change this to a long random string in production)
jwt.secret=SriRathnaMarriageHallSecretKey2024VeryLongAndSecureKey
jwt.expiration=86400000

# CORS
cors.allowed-origins=http://localhost:3000,http://localhost:5173

# App Config
app.admin.mobile=9999999999
app.otp.expiry-minutes=10
app.advance-amount=1000

# Logging
logging.level.com.srirathna=INFO
logging.level.org.hibernate.SQL=WARN
